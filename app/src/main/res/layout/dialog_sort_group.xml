<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/firefly_dialog_background"
    android:padding="0dp">

    <!-- Dialog Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/dialog_header_background"
        android:orientation="horizontal"
        android:padding="20dp"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/dialogTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sort &amp; Group"
                android:textSize="22sp"
                android:textColor="@color/text_primary"
                android:fontFamily="sans-serif-medium" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Organize your videos"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:layout_marginTop="4dp" />

        </LinearLayout>

        <!-- BIGGER Firefly character in header -->
        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/firefly_character"
            android:scaleType="fitCenter"
            android:layout_marginStart="12dp"
            android:contentDescription="@string/firefly_character" />

    </LinearLayout>

    <!-- Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Group By Section -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Group By"
            android:textSize="16sp"
            android:textColor="@color/accent"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="12dp" />

        <RadioGroup
            android:id="@+id/groupByRadioGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:background="@drawable/dialog_options_background"
            android:padding="8dp">

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/groupByNone"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="None"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/groupByFolder"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Folder (Name)"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/groupByDate"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Date (Month/Year)"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/groupBySize"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Size Range"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

        </RadioGroup>

        <!-- Sort By Section - SIMPLIFIED TO JUST ASCENDING/DESCENDING -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sort Order"
            android:textSize="16sp"
            android:textColor="@color/accent"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="12dp" />

        <RadioGroup
            android:id="@+id/sortByRadioGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:background="@drawable/dialog_options_background"
            android:padding="8dp">

            <!-- ASCENDING: Name A-Z, Date Oldest→Newest, Size Smallest→Largest -->
            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/sortByName"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Ascending (A→Z, Old→New, Small→Large)"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

            <!-- DESCENDING: Name Z-A, Date Newest→Oldest, Size Largest→Smallest -->
            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/sortByDate"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Descending (Z→A, New→Old, Large→Small)"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                app:buttonTint="@color/accent"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />

        </RadioGroup>

        <!-- Action Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCancel"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="CANCEL"
                android:textColor="@color/text_secondary"
                android:layout_marginEnd="12dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnApply"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="APPLY"
                android:textColor="@color/white"
                android:backgroundTint="@color/accent"
                app:cornerRadius="12dp" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>